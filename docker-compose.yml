services:
  vmhq-mcp:
    image: ${VMHQ_MCP_IMAGE:-ghcr.io/vmhq/vmhq-mcp:latest}
    build:
      context: .
    env_file:
      - path: .env
        required: false
    ports:
      - "${HOST_PORT:-3010}:${MCP_PORT:-3010}"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

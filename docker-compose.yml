services:
  vmhq-mcp:
    image: ${VMHQ_MCP_IMAGE:-ghcr.io/vmhq/vmhq-mcp:latest}
    build:
      context: .
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-3010}:${MCP_PORT:-3010}"
    restart: unless-stopped
